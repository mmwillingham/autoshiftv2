# Default values for fence-agents-remediation
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
policy_namespace: open-cluster-policies
fenceAgentsRemediation:
  name: fence-agents-remediation-operator
  namespace: openshift-workload-availability
  channel: stable
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  operatorGroupName: openshift-workload-availability
  # targetNamespaces: # Optional: specify target namespaces for namespace-scoped operators
  #   - openshift-workload-availability
  
# hubClusterSets:
#   hub:
#     labels:
#       test1: 'test'
# managedClusterSets:
#   managed:
#     labels:
#       test4: 'test'

### AutoShift Labels Documentation
# The following labels can be set at the cluster or clusterset level to configure this policy:
#
# Enable/Disable:
# fence-agents-remediation<bool>: 'true' or 'false' - Controls whether fence-agents-remediation is managed
#
# Configuration:
# fence-agents-remediation-subscription-name<string>: Operator subscription name (default: 'fence-agents-remediation-operator')
# fence-agents-remediation-channel<string>: Operator channel (default: 'stable')
# fence-agents-remediation-version<string>: Specific operator version (CSV) to install (optional)
# fence-agents-remediation-source<string>: Operator catalog source (default: 'redhat-operators')
# fence-agents-remediation-source-namespace<string>: Catalog namespace (default: 'openshift-marketplace')
#
# Examples:
# autoshift.io/fence-agents-remediation: 'true'
# autoshift.io/fence-agents-remediation-subscription-name: 'fence-agents-remediation-operator'
# autoshift.io/fence-agents-remediation-channel: 'stable'
# autoshift.io/fence-agents-remediation-version: 'operator-name.v1.x.x'
# autoshift.io/fence-agents-remediation-source: 'redhat-operators'
# autoshift.io/fence-agents-remediation-source-namespace: 'openshift-marketplace'