{{- $policyName := "policy-kube-descheduler-config" }}
{{- $placementName := "placement-policy-kube-descheduler-config" }}

apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: {{ $policyName }}
  namespace: {{ .Values.policy_namespace }}
  annotations:
    policy.open-cluster-management.io/standards: NIST SP 800-53
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
spec:
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: kube-descheduler-instance
        spec:
          remediationAction: enforce
          severity: high
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: operator.openshift.io/v1
                kind: KubeDescheduler
                metadata:
                  name: cluster
                  namespace: openshift-kube-descheduler-operator
                spec:
                  deschedulingIntervalSeconds: 3600
                  managementState: Managed
                  mode: Predictive
                  profiles:
                    - KubeVirtRelieveAndMigrate
                  profileCustomizations:
                    devEnableEvictionsInBackground: true
    
# This requires PSI metrics. To check/verify it is enabled:
# oc debug node/<worker-node-name>
# Inside the debug shell:
# chroot /host
# cat /proc/pressure/cpu
# To enable, apply this yaml
#apiVersion: machineconfiguration.openshift.io/v1
#kind: MachineConfig
#metadata:
#  labels:
#    machineconfiguration.openshift.io/role: worker
#  name: 06-worker-psi
#spec:
#  config:
#    ignition:
#      version: 3.2.0
#  kernelArguments:
#    - psi=1

# You also must ensure VMs have this:
#spec:
#  template:
#    metadata:
#      annotations:
#        descheduler.alpha.kubernetes.io/evict: "true"

#                    - AffinityAndTaints
#                    - TopologyAndDuplicates
#                    - LifecycleAndUtilization
#                    - DevPreviewLongLifecycle
#                    - LongLifecycle
#                    - SoftTopologyAndDuplicates
#                    - EvictPodsWithLocalStorage
#                    - EvictPodsWithPVC
#                    - CompactAndScale
#                    - DevKubeVirtRelieveAndMigrate
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: {{ $placementName }}
  namespace: {{ .Values.policy_namespace }}
spec:
  clusterSets:
  {{- range $clusterSet, $value := $.Values.hubClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  {{- range $clusterSet, $value := $.Values.managedClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/kube-descheduler'
              operator: In
              values:
              - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: {{ $placementName }}
  namespace: {{ .Values.policy_namespace }}
placementRef:
  name: {{ $placementName }}
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: {{ $policyName }}
    apiGroup: policy.open-cluster-management.io
    kind: Policy
